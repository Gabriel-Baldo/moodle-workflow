services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ~/.config/opencode/moodle.env:/app/.env:ro
    env_file:
      - .env
    restart: unless-stopped